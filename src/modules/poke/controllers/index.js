(function(angular) {
    'use strict';

    angular.module('poke')
        .controller('poke.controllers.index', ['$scope', 'poke.services.pokemons', func]);

    function func($scope, service) {


    }
})(angular);
